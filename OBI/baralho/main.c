#include <stdio.h>
#include <string.h>

char * split(char * input[157]) {
  printf("INPUT=%s\n", input);
  char output[3][4];
  
  return input;
}

int main() {
  char input[157];

  scanf("%s", input);
split(input);  return 0;
}